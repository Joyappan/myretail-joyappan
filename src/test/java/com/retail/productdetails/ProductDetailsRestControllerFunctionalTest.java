package com.retail.productdetails;

/*import org.junit.Rule;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.web.client.TestRestTemplate;
import org.springframework.test.context.junit4.SpringRunner;

import com.github.tomakehurst.wiremock.core.WireMockConfiguration;
import com.github.tomakehurst.wiremock.junit.WireMockRule;

@RunWith(SpringRunner.class)
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)*/
public class ProductDetailsRestControllerFunctionalTest {
	/*@Rule
	public WireMockRule wireMockRule = new WireMockRule(WireMockConfiguration.options().dynamicPort());

	@Autowired
	private TestRestTemplate restTemplate;

	@Test
	    public void createClient() {

	    propertyReader.externalurl = "http://localhost:"+wiremock.G+"/product/v1;
	    stubFor(get(urlEqualTo("/api/resource/"))
	                .willReturn(aResponse()
	                .withStatus(HttpStatus.OK.value())
	                .withHeader("Content-Type", TEXT_PLAIN_VALUE)
	                .withBody("test")));

	        
	        ResponseEntity<ProductDetailsRequest> responseEntity =
	            restTemplate.getForEntity("/v1/product/123",ProductDetailsRequest.class);
	        ProductDetailsRequest result = responseEntity.getBody();
	        assertEquals(HttpStatus.CREATED, responseEntity.getStatusCode());
	        assertEquals("Foo", result.getName());
	    }
*/
}
